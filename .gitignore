.idea
dist/client/system
.claude
.worktrees
target/
tools/vector-dump/out/
*.db
*.db-shm
*.db-wal
dist/game/log/*
dist/login/log/*
**/.DS_Store

# Personal deploy tooling — points at a specific operator's server, and holds
# credentials (deploy-web.sh carries a Cloudflare API token).
deploy.sh
deploy-dashboard.sh
deploy-web.sh
deploy.env
# Launcher build-time config: points a build at a specific deployment
crates/launcher/.env

# The plaintext mirror `l2r-tools client-dat` unpacks the client into. The
# client itself is ignored at the top of this file; both are bulk NCsoft data,
# not ours to redistribute.
dist/client/system_decrypted

# Readable text mirror produced by `l2r-tools dat-text unpack`.
dist/client/system_text
