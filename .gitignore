.idea
dist/client/system
.claude
.worktrees
target/
tools/vector-dump/out/
*.db
*.db-shm
*.db-wal
dist/game/log/*
dist/login/log/*
**/.DS_Store

# Personal deploy tooling — points at a specific operator's server, and holds
# credentials (deploy-web.sh carries a Cloudflare API token).
deploy.sh
deploy-dashboard.sh
deploy-web.sh
deploy.env
# Launcher build-time config: points a build at a specific deployment
crates/launcher/.env

# The game client, and the plaintext mirror tools/l2_dat_cipher.py unpacks it
# into — both are bulk NCsoft data, not ours to redistribute.
dist/client/system
dist/client/system_decrypted
